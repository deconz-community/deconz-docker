---
name: Container build gate

on:
  pull_request:
  workflow_dispatch:

concurrency:
  group: container-ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build-smoke-scan:
    name: ${{ matrix.base.name }} / ${{ matrix.target.platform }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        base:
          - name: bookworm
            image: debian:12-slim@sha256:7b140f374b289a7c2befc338f42ebe6441b7ea838a042bbd5acbfca6ec875818
          - name: trixie
            image: debian:13-slim@sha256:020c0d20b9880058cbe785a9db107156c3c75c2ac944a6aa7ab59f2add76a7bd
        target:
          - name: amd64
            platform: linux/amd64
          - name: arm64
            platform: linux/arm64
          - name: armv7
            platform: linux/arm/v7
    env:
      IMAGE_TAG: deconz-ci:${{ matrix.base.name }}-${{ matrix.target.name }}
      CONTAINER_NAME: deconz-ci-${{ matrix.base.name }}-${{ matrix.target.name }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Read build inputs
        id: inputs
        run: |
          echo "version=$(jq -r '.version' version.json)" >> "$GITHUB_OUTPUT"
          echo "channel=$(jq -r '.channel' version.json)" >> "$GITHUB_OUTPUT"

      - name: Build local image
        uses: docker/build-push-action@v5
        with:
          context: ./docker/
          file: docker/Dockerfile
          platforms: ${{ matrix.target.platform }}
          load: true
          push: false
          tags: ${{ env.IMAGE_TAG }}
          build-args: |
            BASE_IMAGE=${{ matrix.base.image }}
            VERSION=${{ steps.inputs.outputs.version }}
            CHANNEL=${{ steps.inputs.outputs.channel }}

      - name: Smoke test container
        run: |
          set -eu
          appdata=$(mktemp -d)
          cleanup() {
            docker logs "$CONTAINER_NAME" || true
            docker inspect "$CONTAINER_NAME" || true
            docker rm -f "$CONTAINER_NAME" || true
            rm -rf "$appdata"
          }
          trap cleanup EXIT

          docker run -d --name "$CONTAINER_NAME" \
            -p 127.0.0.1::80 \
            -v "$appdata:/opt/deCONZ" \
            -e DECONZ_DEVICE=0 \
            -e DECONZ_VNC_MODE=0 \
            -e DECONZ_UPNP=0 \
            "$IMAGE_TAG"

          for attempt in $(seq 1 48); do
            if [ "$(docker inspect -f '{{.State.Running}}' "$CONTAINER_NAME")" != true ]; then
              echo "Container stopped before becoming ready"
              exit 1
            fi
            if docker exec "$CONTAINER_NAME" curl --fail --silent --show-error --max-time 10 http://127.0.0.1:80/ >/dev/null; then
              exit 0
            fi
            sleep 5
          done
          echo "Container did not become HTTP-ready within four minutes"
          exit 1

      - name: Scan local image
        uses: anchore/scan-action@v6
        with:
          image: ${{ env.IMAGE_TAG }}
          severity-cutoff: high
          fail-build: true
